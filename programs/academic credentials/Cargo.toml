[package]
name = "academic_credentials"
version = "0.1.0"
description = "Solana on-chain academic credential validation"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "academic_credentials"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.29.0"